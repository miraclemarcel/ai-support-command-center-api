import { Body, Controller, Get, Param, Post, UseGuards } from '@nestjs/common';
import { ApiBearerAuth, ApiTags } from '@nestjs/swagger';
import { CurrentUser } from '../common/decorators/current-user.decorator'; import { JwtAuthGuard } from '../common/guards/jwt-auth.guard';
import { ConversationsService } from './conversations.service'; import { CreateConversationDto, CreateMessageDto } from './dto';
@ApiTags('Conversations') @ApiBearerAuth() @UseGuards(JwtAuthGuard) @Controller('conversations') export class ConversationsController { constructor(private service:ConversationsService){} @Post() create(@CurrentUser()u:any,@Body()d:CreateConversationDto){return this.service.create(u.organizationId,d)} @Get() list(@CurrentUser()u:any){return this.service.list(u.organizationId)} @Get(':id') get(@CurrentUser()u:any,@Param('id')id:string){return this.service.get(u.organizationId,id)} @Post(':id/messages') message(@CurrentUser()u:any,@Param('id')id:string,@Body()d:CreateMessageDto){return this.service.addMessage(u.organizationId,id,d,u.id)} @Post(':id/ai-reply') ai(@CurrentUser()u:any,@Param('id')id:string){return this.service.aiReply(u.organizationId,id)} @Post(':id/takeover') takeover(@CurrentUser()u:any,@Param('id')id:string){return this.service.takeover(u.organizationId,id)} }
